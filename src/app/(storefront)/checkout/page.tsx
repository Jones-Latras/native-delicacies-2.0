import { CheckoutClient } from "./checkout-client";

export const metadata = {
  title: "Checkout | Native Delicacies",
  description: "Complete your order",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
